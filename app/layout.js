import './globals.css';

export const metadata = {
  title: 'ScoutOP Follower Tracker',
  description: 'Minimal Instagram follower growth tracker for @scoutop'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
